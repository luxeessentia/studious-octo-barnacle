import Layout from '../../components/Layout';
export default function ReturnRequest(){ return (<Layout><h1>Request a Return</h1><form onSubmit={(e)=>{ e.preventDefault(); const req={ id:'RR-'+Date.now(), order:e.target.order.value, email:e.target.email.value, reason:e.target.reason.value, status:'Pending' }; const arr=JSON.parse(localStorage.getItem('returns')||'[]'); arr.push(req); localStorage.setItem('returns', JSON.stringify(arr)); alert('Return requested'); e.target.reset(); }}><input name='order' placeholder='Order number' required style={{width:'100%',padding:8,marginBottom:8}}/><input name='email' type='email' placeholder='Email' required style={{width:'100%',padding:8,marginBottom:8}}/><select name='reason' style={{width:'100%',padding:8,marginBottom:8}}><option>Wrong size</option><option>Damaged</option><option>Other</option></select><textarea name='notes' placeholder='Notes' style={{width:'100%',padding:8,marginBottom:8}}/><button className='btn' type='submit'>Submit Return Request</button></form></Layout>); }